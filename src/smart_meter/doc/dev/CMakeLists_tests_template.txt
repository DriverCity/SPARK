# Do not change
cmake_minimum_required(VERSION 2.4)

# Name is not important. Give any unique name you like.
project(mylibtest)

# Get access to component-under-test's headers.
include_directories(../include)

# List test target's source files here.
set(TEST_SRCS 
  ../src/module1.cpp)
  ../src/module2.cpp
)

# List test sources here
set(SRCS 
  module1test.cpp
  module2test.cpp
)

# Link other components (omit if no dependencies)
include_directories($ENV{BUILD_DIR}/include)
set(LINKED_LIBS lib1 lib2 lib3)
foreach(lib ${LINKED_LIBS})
    link_directories($ENV{BUILD_DIR}/${lib}/bin)
endforeach(lib)

# Do not change
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR})
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR})
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR})

add_executable(${PROJECT_NAME} ${TEST_SRCS} ${SRCS})
target_link_libraries(${PROJECT_NAME} gtest)